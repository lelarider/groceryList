{
  "name": "grocerylist",
  "version": "1.0.0",
  "description": "Create grocery list ordered by departmnet",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon ./server.js",
    "ui": "browser-sync start --proxy=localhost:8000 --files='**/*.css, **/*.ejs, **/*.js' --ignore=node_modules --reload-delay 10 --no-ui --no-notify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ilissa Blech",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "ejs": "^3.0.1",
    "esm": "^3.2.25",
    "express": "^4.17.1",
    "yargs": "^15.0.2"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "ejs-lint": "^0.3.0",
    "nodemon": "^2.0.2"
  }
}
